{
  "name": "boisepropertygroup",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "check:evidence": "node scripts/validation/check-evidence.mjs",
    "check:redteam": "node scripts/validation/red-team-arithmetic.mjs",
    "check:quotes": "node scripts/validation/verify-quotes.mjs",
    "check:commercial": "node scripts/validation/check-commercial.mjs",
    "check:vocabulary": "node scripts/validation/check-vocabulary.mjs",
    "check:pages": "node scripts/validation/check-commercial-pages.mjs",
    "check:links": "node scripts/validation/check-inline-links.mjs",
    "check:local": "node scripts/codegen/check-drift.mjs && node scripts/validation/check-evidence.mjs && node scripts/validation/red-team-arithmetic.mjs && node scripts/validation/verify-quotes.mjs && node scripts/validation/check-commercial.mjs && node scripts/validation/check-vocabulary.mjs && node scripts/validation/check-pages-dry.mjs && node scripts/validation/check-inline-links.mjs",
    "check:all": "npm run typecheck && npm run test && npm run check:drift && npm run check:evidence && npm run check:redteam && npm run check:quotes && npm run check:commercial && npm run check:vocabulary && npm run check:pages",
    "check:drift": "node scripts/codegen/check-drift.mjs"
  },
  "dependencies": {
    "next": "^15.3.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "resend": "^6.9.4"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.0.0",
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "postcss": "^8.5.0",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.0.0",
    "vitest": "^2.1.0"
  }
}
